"""External integrations (ClearML, etc).

This package is the target location for integration layers that should stay
thin and isolate third-party SDK usage from core/workflow code.

During the refactor we keep backward-compatible wrappers under `automl_lib/clearml`
and progressively move code here.
"""

